{
  "name": "@hackboard/shared-schemas",
  "version": "0.1.0",
  "private": true,
  "description": "JSON Schemas compartidos entre api y cli: eventos de ingesta y salida de la IA.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "schemas"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "typescript": "^5.6.0",
    "vitest": "^5.0.1"
  }
}
