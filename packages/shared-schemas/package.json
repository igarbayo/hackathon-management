{
  "name": "@hackboard/shared-schemas",
  "version": "0.1.0",
  "private": true,
  "description": "JSON Schemas shared by api and cli: ingest events and AI output.",
  "license": "AGPL-3.0-or-later",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "schemas"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
