source "https://rubygems.org"

# Bundle edge Rails instead: gem "rails", github: "rails/rails", branch: "main"
gem "rails", "~> 8.1.3", ">= 8.1.3.1"
# Use the Puma web server [https://github.com/puma/puma]
gem "puma", ">= 5.0"
# Build JSON APIs with ease [https://github.com/rails/jbuilder]
# gem "jbuilder"

# Use Active Model has_secure_password [https://guides.rubyonrails.org/active_model_basics.html#securepassword]
gem "bcrypt", "~> 3.1.7"

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem "tzinfo-data", platforms: %i[ windows jruby ]

# Add HTTP asset caching/compression and X-Sendfile acceleration to Puma [https://github.com/basecamp/thruster/]
gem "thruster", require: false

# Use Rack CORS for handling Cross-Origin Resource Sharing (CORS), making cross-origin Ajax possible
gem "rack-cors"

# Persistencia: MongoDB en lugar de Active Record (ADR-0001)
gem "mongoid", "~> 9.0"

# Jobs en background (ADR-0002)
gem "sidekiq", "~> 7.3"
gem "sidekiq-cron", "~> 2.1"
# connection_pool 3.x cambió la firma de #pop y rompe el hilo interno de
# Sidekiq::Scheduled ("wrong number of arguments") con sidekiq 7.3.x.
gem "connection_pool", "~> 2.5"

# Validación de JSON Schema para ingesta y salida de IA (01-arquitectura)
gem "json_schemer", "~> 2.3"

# Fijado a la versión que trae de serie la imagen de Ruby: bundler resolvía
# json 3.0.2 (pedido por json_schemer con ">= 2.3"), cuya extensión nativa
# no coincide con la que ya carga esta imagen y provocaba
# `ArgumentError: wrong number of arguments` en cualquier JSON.parse.
gem "json", "2.7.2"

# Cliente HTTP para GitHub API y Gemini API
gem "faraday", "~> 2.9"
gem "faraday-retry", "~> 2.2"

# Verificación del ID token de Google (login OpenID Connect, ADR-0011)
gem "jwt", "~> 2.9"

# Variables de entorno en desarrollo y test
gem "dotenv-rails", groups: [:development, :test]

group :development, :test do
  # See https://guides.rubyonrails.org/debugging_rails_applications.html#debugging-with-the-debug-gem
  gem "debug", platforms: %i[ mri windows ], require: "debug/prelude"

  # Audits gems for known security defects (use config/bundler-audit.yml to ignore issues)
  gem "bundler-audit", require: false

  # Static analysis for security vulnerabilities [https://brakemanscanner.org/]
  gem "brakeman", require: false

  # Omakase Ruby styling [https://github.com/rails/rubocop-rails-omakase/]
  gem "rubocop-rails-omakase", require: false
end

group :test do
  gem "rspec-rails", "~> 7.1"
  gem "factory_bot_rails", "~> 6.4"
  gem "webmock", "~> 3.24"
end
